# Copy to .psxrecomp.env (gitignored). Relative paths resolve from repo root.
# Each game gets <root>/<game-id>/overlay_captures.json plus additive .json.d/.
PSX_OVERLAY_CAPTURE_ROOT=.local/overlay-captures
